<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.zju.gis.dldsj.server.mapper.LiteratureMapper">
    <resultMap id="BaseResultMap" type="edu.zju.gis.dldsj.server.entity.Literature">
        <result column="ID" jdbcType="VARCHAR" property="id"/>
        <result column="TITLE" jdbcType="VARCHAR" property="title"/>
        <result column="AUTHOR" jdbcType="VARCHAR" property="author"/>
        <result column="TIME" jdbcType="TIMESTAMP" property="time"/>
        <result column="KEYWORD" jdbcType="VARCHAR" property="keyword"/>
        <result column="CLASSIFICATION" jdbcType="VARCHAR" property="classification"/>
        <result column="SOURCE" jdbcType="VARCHAR" property="source"/>
        <result column="DOWNLOADS" jdbcType="VARCHAR" property="downloads"/>
        <result column="PATH" jdbcType="VARCHAR" property="path"/>
        <result column="URL" jdbcType="VARCHAR" property="url"/>
    </resultMap>
    <sql id="Base_Column_List">
        ID, TITLE, AUTHOR, TIME, KEYWORD, CLASSIFICATION, SOURCE, DOWNLOADS , PATH , URL
    </sql>
    <sql id="TABLE_NAME">
        tb_academic_achievements
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from <include refid="TABLE_NAME"/>
        where ID = #{id,jdbcType=VARCHAR}
    </select>

    <!--  数据目录中的功能  -->
    <select id="selectBySource" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from <include refid="TABLE_NAME"/>
        where SOURCE = #{source,jdbcType=VARCHAR}
    </select>


    <!--  查找功能  -->
    <select id="search" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM <include refid="TABLE_NAME"/>
        <where>
            <if test="title!=null and title.length() &gt; 0">AND TITLE like "%${title}%"</if>
            <if test="author!=null and author.length() &gt; 0">AND AUTHOR like "%${author}%"</if>
            <if test="source!=null and source.length() &gt; 0">AND SOURCE like "%${source}%"</if>
            <if test="keyword!=null and keyword.length() &gt; 0">AND KEYWORD like "%${keyword}%"</if>
            <if test="classification!=null and classification.length() &gt; 0">AND CLASSIFICATION like "%${classification}%"</if>
        </where>
    </select>

    <!--  查询不重复的字段名  -->
    <select id="getDistinctField" parameterType="java.lang.String" resultMap="BaseResultMap" statementType="STATEMENT">
        SELECT DISTINCT
        ${_parameter}
        FROM <include refid="TABLE_NAME"/>
    </select>
    <!--  根据某一字段名返回其个数  -->
    <select id="getCountOfField" parameterType="String" resultType="String" statementType="STATEMENT">
        SELECT COUNT(*)
        FROM <include refid="TABLE_NAME"/>
        WHERE ${arg0} = '${arg1}'
    </select>

    <!-- 时间段查询传参Date -->
    <select id="selectByTime" resultMap="BaseResultMap" parameterType="Integer" >
        select
        <include refid="Base_Column_List"/>
        from <include refid="TABLE_NAME"/>
        <where>
            <if test="arg0!=null and arg0!=''">
                cast(TIME as DECIMAL)<![CDATA[>=]]> #{arg0,jdbcType=INTEGER}
            </if>
            <if test="arg1!=null and arg1!=''">
                and cast(TIME as DECIMAL) <![CDATA[<=]]> #{arg1,jdbcType=INTEGER}
            </if>
        </where>
    </select>



</mapper>
